// write a program to fing area of reqtengle.
#include<stdio.h>
void main()
{
    int height, width,area;
    area = 0;
    height = 20;
    width = 25;

    printf("height is %d and width is %d\n", height, width);
    area = height * width;
    printf("Area is %d ", area);
}